org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Flutter 3.44 requires this AGP 9 host to keep Built-in Kotlin disabled.
# The published plugin also supports Flutter 3.47+ hosts that enable Built-in Kotlin.
# Remove this opt-out when repository validation moves to Flutter 3.47+.
android.builtInKotlin=false
android.newDsl=false
