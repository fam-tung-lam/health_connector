name: health_connector_hk_ios
resolution: workspace
description: iOS implementation of health_connector using HealthKit
version: 3.10.1
repository: https://github.com/fam-tung-lam/health_connector/tree/main/packages/health_connector_hk_ios
issue_tracker: https://github.com/fam-tung-lam/health_connector/issues?q=is%3Aissue%20is%3Aopen

environment:
  sdk: ^3.10.0
  flutter: '>=3.38.0'

dependencies:
  flutter:
    sdk: flutter
  health_connector_core: ^3.10.1
  health_connector_logger: ^4.0.2
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  health_connector_lint: ^1.2.2
  mocktail: ^1.0.4
  parameterized_test: ^2.0.2
  pigeon: ^26.1.0

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: HealthConnectorHkIosPlugin

topics:
  - health
  - ios-healthkit
